[package]
name = "slint-webview-servo"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"
description = "Servo texture backend for Slint webview composition."
license = "LGPL-3.0-only"
readme = "README.md"
repository = "https://github.com/KTheMan/slint-webview"
publish = false

[dependencies]
slint-webview-core = { path = "../core" }
